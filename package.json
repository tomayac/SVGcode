{
  "name": "SVGcode",
  "version": "0.0.1",
  "scripts": {
    "start": "npm run dev",
    "dev": "vite",
    "build": "vite build --base=/SVGcode/",
    "serve": "vite preview",
    "lint": "npx eslint . --ext .js,.mjs --fix --ignore-pattern docs/",
    "fix": "npx prettier . --write"
  },
  "dependencies": {
    "browser-fs-access": "^0.16.4",
    "potrace-wasm": "github:tomayac/potrace-wasm#patch-1",
    "svgo": "^2.3.0"
  },
  "devDependencies": {
    "eslint": "^7.23.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^8.1.0",
    "prettier": "^2.2.1",
    "vite": "^2.1.5",
    "vite-plugin-pwa": "^0.6.5"
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 2020,
      "sourceType": "module"
    },
    "extends": [
      "google",
      "prettier"
    ],
    "rules": {
      "valid-jsdoc": "off"
    }
  }
}
